---
title: "NaverMapGroundOverlay"
description: "Component for displaying ground overlays on the map"
---

## Overview

NaverMapGroundOverlay는 지도 위에 그라운드 오버레이를 표시하는 컴포넌트입니다.
특정 지리적 영역에 이미지를 오버레이하여 표시할 수 있습니다.

## Basic Usage

```tsx
import { NaverMapView, NaverMapGroundOverlay } from '@mj-studio/react-native-naver-map';

function MapWithGroundOverlay() {
  return (
    <NaverMapView style={{ flex: 1 }}>
      <NaverMapGroundOverlay // [!code highlight]
        image={{ assetName: 'thumbnail' }} // [!code highlight]
        region={{ // [!code highlight]
          latitude: 37.5, // [!code highlight]
          longitude: 126.9, // [!code highlight]
          latitudeDelta: 0.1, // [!code highlight]
          longitudeDelta: 0.1, // [!code highlight]
        }} // [!code highlight]
        onTap={() => console.log('Ground overlay tapped')} // [!code highlight]
      /> // [!code highlight]
    </NaverMapView>
  );
}
```

## Props

`NaverMapGroundOverlayProps`

<AutoTypeTable path="../src/component/NaverMapGroundOverlay.tsx" name="NaverMapGroundOverlayProps" />
