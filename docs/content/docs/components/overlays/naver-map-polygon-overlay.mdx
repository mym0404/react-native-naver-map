---
title: "NaverMapPolygonOverlay"
description: "Component for displaying polygon overlays on the map"
---

## Overview

NaverMapPolygonOverlay는 지도 위에 폴리곤(다각형) 오버레이를 표시하는 컴포넌트입니다.
좌표 배열을 통해 다각형 영역을 정의하고, 채우기 색상과 외곽선 스타일을 설정할 수 있습니다.

## Basic Usage

```tsx
import { NaverMapView, NaverMapPolygonOverlay } from '@mj-studio/react-native-naver-map';

function MapWithPolygon() {
  return (
    <NaverMapView style={{ flex: 1 }}>
      <NaverMapPolygonOverlay // [!code highlight]
        coords={[ // [!code highlight]
          { latitude: 37.5, longitude: 126.9 }, // [!code highlight]
          { latitude: 37.51, longitude: 126.91 }, // [!code highlight]
          { latitude: 37.52, longitude: 126.9 }, // [!code highlight]
          { latitude: 37.51, longitude: 126.89 }, // [!code highlight]
        ]} // [!code highlight]
        color={'#00ff0033'} // [!code highlight]
        outlineColor={'#00ff00'} // [!code highlight]
        outlineWidth={2} // [!code highlight]
        onTap={() => console.log('Polygon tapped')} // [!code highlight]
      /> // [!code highlight]
    </NaverMapView>
  );
}
```

## Props

`NaverMapPolygonOverlayProps`

<AutoTypeTable path="../src/component/NaverMapPolygonOverlay.tsx" name="NaverMapPolygonOverlayProps" />
