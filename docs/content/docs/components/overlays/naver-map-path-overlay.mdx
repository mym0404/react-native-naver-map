---
title: "NaverMapPathOverlay"
description: "Component for displaying path overlays on the map"
---

## Overview

NaverMapPathOverlay는 지도 위에 경로 오버레이를 표시하는 컴포넌트입니다.
좌표 배열을 통해 경로를 표시하며, 경로의 색상, 두께, 패턴 등을 설정할 수 있습니다.

## Basic Usage

```tsx
import { NaverMapView, NaverMapPathOverlay } from '@mj-studio/react-native-naver-map';

function MapWithPath() {
  return (
    <NaverMapView style={{ flex: 1 }}>
      <NaverMapPathOverlay // [!code highlight]
        coords={[ // [!code highlight]
          { latitude: 37.5, longitude: 126.9 }, // [!code highlight]
          { latitude: 37.51, longitude: 126.91 }, // [!code highlight]
          { latitude: 37.52, longitude: 126.92 }, // [!code highlight]
        ]} // [!code highlight]
        width={8} // [!code highlight]
        color={'white'} // [!code highlight]
        progress={0.5} // [!code highlight]
        passedColor={'black'} // [!code highlight]
        outlineWidth={1} // [!code highlight]
        onTap={() => console.log('Path tapped')} // [!code highlight]
      /> // [!code highlight]
    </NaverMapView>
  );
}
```

## Props

`NaverMapPathOverlayProps`

<AutoTypeTable path="../src/component/NaverMapPathOverlay.tsx" name="NaverMapPathOverlayProps" />
