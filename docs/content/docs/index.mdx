---
title: "React Native Naver Map"
description: "React Native용 네이버 지도 라이브러리 - New Architecture (Fabric) 지원"
icon: "Map"
---

import { Callout } from 'fumadocs-ui/components/callout'
import { Cards, Card } from 'fumadocs-ui/components/card'

# React Native Naver Map

<Callout type="info">
  **v2.x 버전**부터는 React Native의 New Architecture (Fabric)만 지원합니다.
  Bridge 아키텍처를 사용 중이라면 v1.x 버전을 사용해주세요.
</Callout>

# 주요 특징



## 상세한 문서화


모든 컴포넌트와 API에 대한 완벽한 TypeScript 타입 정의와 JSDoc 주석을 제공합니다. IDE에서 자동 완성과 인라인 문서를 통해 개발 생산성을 높일 수 있습니다.



## Expo 지원


Expo Config Plugin을 통해 Expo 프로젝트에서도 손쉽게 사용할 수 있습니다.



## 최적화된 마커 성능


다양한 마커 타입을 지원하며, 이미지 캐싱과 최적화를 통해 수천 개의 마커도 부드럽게 렌더링합니다.

지원하는 마커 이미지 타입:
- **기본 심볼**: 네이버 지도 제공 기본 마커 (green, red, gray 등)
- **로컬 리소스**: React Native 프로젝트의 이미지 (`require()`)
- **네이티브 에셋**: Android Drawable, iOS Bundle Asset으로 최적화된 성능
- **웹 이미지**: HTTP/HTTPS URL을 통한 네트워크 이미지
- **커스텀 뷰**: `children` prop으로 전달하는 React Native 컴포넌트



## 네이티브 SDK


최신 네이버 지도 SDK 기능을 React Native에서 사용할 수 있도록 포팅했습니다.


# 빠른 시작

```tsx
import { NaverMapView, NaverMapMarkerOverlay } from '@mj-studio/react-native-naver-map';

export default function MapScreen() {
  return (
    <NaverMapView
      style={{ flex: 1 }}
      initialRegion={{
        latitude: 37.5665,
        longitude: 126.9780,
        latitudeDelta: 0.01,
        longitudeDelta: 0.01,
      }}
    >
      <NaverMapMarkerOverlay
        latitude={37.5665}
        longitude={126.9780}
        anchor={{ x: 0.5, y: 1 }}
        caption={{ text: "서울시청" }}
      />
    </NaverMapView>
  );
}
```

<Callout type="warn">
  네이버 지도를 사용하려면 [네이버 클라우드 플랫폼](https://www.ncloud.com)에서 Maps API 키를 발급받아야 합니다.
</Callout>

# 버전 호환성

| React Native Naver Map | React Native | 비고                                |
|------------------------|--------------|-----------------------------------|
| ≥ 2.1.0                | ≥ 0.74       | Bridge 지원 중단, New Architecture 필수 |
| ≥ 2.0.0                | ≥ 0.73       | New Architecture 전용               |
| < 2.0.0                | ≥ 0.64       | Old/New Architecture 모두 지원        |

<Callout>
  2025년 4월 17일부터 [새로운 Maps 단독 상품](https://www.ncloud.com/support/notice/all/1930)이 출시되었습니다.
  기존 AI NAVER API 고객은 레거시 키 설정을 참고해주세요.
</Callout>
