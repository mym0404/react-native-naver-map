---
title: "Getting Started"
description: "React Native용 네이버 지도 라이브러리"
icon: "Map"
---

import { Apple, Smartphone, Zap } from 'lucide-react'
import { Callout } from 'fumadocs-ui/components/callout'
import { Cards, Card } from 'fumadocs-ui/components/card'

## 시작하기

React Native Naver Map을 프로젝트에 설치하고 설정하는 방법을 안내합니다.

## 사전 요구사항

- New Architecture
- [네이버 클라우드 플랫폼](https://www.ncloud.com) 계정 및 Maps API 키

## 설치

```npm
npm install @mj-studio/react-native-naver-map
```


## API 키 발급

### 네이버 클라우드 플랫폼 접속

[네이버 클라우드 플랫폼 콘솔](https://console.ncloud.com)에 로그인합니다.

### Maps 상품 신청

- AI·Application Service > Maps 메뉴로 이동
- "이용 신청하기" 클릭
- Application 등록

### Client ID 발급

- Application 이름 입력
- 서비스 환경 선택 (Mobile)
- iOS Bundle ID, Android Package Name 등록
- Client ID 복사

### 키 설정

발급받은 Client ID를 각 플랫폼별로 설정합니다.

## 플랫폼별 설정

설치가 완료되면 플랫폼별로 추가 설정이 필요합니다. 해당하는 플랫폼의 가이드를 따라 설정해주세요.

<Cards className={'mb-8'}>
  <Card href="/installation/android" title="Android 설정" icon={<Smartphone/>} />
  <Card href="/installation/ios" title="iOS 설정" icon={<Apple/>} />
  <Card href="/installation/expo" title="Expo 설정" icon={<Zap/>} />
</Cards>

## 첫 번째 지도 만들기

플랫폼별 설정이 완료되면 지도를 표시할 수 있습니다:

```tsx title="App.tsx"
import React from 'react';
import { StyleSheet, View } from 'react-native';
import { NaverMapView } from '@mj-studio/react-native-naver-map';

export default function App() {
  return (
    <View style={styles.container}>
      <NaverMapView
        style={styles.map}
        initialRegion={{
          latitude: 37.5665,
          longitude: 126.9780,
          latitudeDelta: 0.01,
          longitudeDelta: 0.01,
        }}
        mapType="Basic"
        isShowLocationButton={true}
        isShowCompass={true}
        isShowScaleBar={true}
      />
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  map: {
    flex: 1,
  },
});
```

<Callout type="success">
축하합니다! React Native 앱에 네이버 지도를 성공적으로 통합했습니다. 이제 다양한 컴포넌트와 기능을 추가해보세요.
</Callout>

