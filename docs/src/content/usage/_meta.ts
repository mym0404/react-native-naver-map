import type { MetaRecord } from 'nextra';

const meta: MetaRecord = {
  init: 'Initialization',
  bump: 'Bump versions',
  pull: 'Download metadatas',
  build: 'Build artifacts',
  upload: 'Upload artifacts to test tracks',
  submit: 'Submit store reivews',
};

export default meta;
