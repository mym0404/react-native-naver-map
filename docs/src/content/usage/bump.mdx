# bump

**Bump binary release patch version with modifying expo config file**

```shell
npx expo-release-it bump
```

`bump` allows you to execute `git` commands like `commit`, `tag`, `push` for version management.
